import { ButtonVariants } from '../Button/types';
import { DefaultParticleProps } from '../types';

export interface ButtonGroupContextProps extends DefaultParticleProps {
  variant?: ButtonVariants;
}
